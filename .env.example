# Default environment configuration for the Hospital Management backend
# Copy this file to .env and adjust values as needed.

# Database connection driver: sqlite or pgsql
DB_CONNECTION=pgsql

# PostgreSQL connection settings (used when DB_CONNECTION=pgsql)
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=hospital_mgmt
DB_USERNAME=hospital_app
DB_PASSWORD=StrongPassword123

# SQLite fallback (used when DB_CONNECTION=sqlite)
# DB_PATH overrides the default ./backend/data/database.sqlite when set
DB_PATH=backend/data/database.sqlite

# Additional optional settings can go here, e.g. MPESA credentials
